<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://www.mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="test.orm.source.dao.BookDao">
    <insert id="add">
        insert into book(name)
        values(#{name})
    </insert>
</mapper>